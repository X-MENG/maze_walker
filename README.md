# maze_walker
使用说明：
1，启动时首先进入编辑模式，按上下左右键控制游标，在空白出按空格键绘制格子，在格子上按空格键取消绘制。
2，按F1键保存编辑结果。
3，将游标移动到已编辑好的格子上，按B键将其标记为起点位置，按E键将其标记为终点位置。
4，按2键进入训练模式，所有的格子都变红表示训练完成。
5，所有格子都变红后按3键进入测试模式，用鼠标任意选择一个格子，agent会自动寻路到目标位置。
